---
import BaseLayout from '../../layouts/BaseLayout.astro';

const code1 = `:root {
  --theme: dark;
  color-scheme: var(--theme);
}

body {
  background: light-dark(#faf8f3, #0c0c14);
  color:      light-dark(#3d3226, #e4e4e7);
}`;

const code2 = `&lt;!-- &#103;这一行就能切换主题 --&gt;
&lt;input type="checkbox" class="theme-toggle"&gt;

/* &#103;当 checkbox 被勾选，:root 上的 --theme 切换为 light */
:root:has(.theme-toggle:checked) {
  --theme: light;
}`;
---

<BaseLayout title="用纯 CSS 实现一个丝滑的主题切换" description="不需要 JavaScript，用 CSS 自定义属性和 :has() 选择器就能做主题切换。">
  <article>
    <a href="/blog" class="text-xs text-text-dim no-underline hover:text-white transition-colors inline-block mb-8">← 返回 Blog</a>

    <div class="flex gap-2 mb-5">
      <span class="font-mono text-xs px-3.5 py-1 rounded-full bg-accent/10 text-accent border border-accent/20">CSS</span>
      <span class="font-mono text-xs px-3.5 py-1 rounded-full bg-accent/10 text-accent border border-accent/20">前端</span>
    </div>

    <h2 class="font-serif text-3xl md:text-4xl font-bold text-white leading-snug mb-3 tracking-tight">
      用纯 CSS 实现一个丝滑的主题切换
    </h2>
    <div class="font-mono text-sm text-text-dim mb-12">2026 年 5 月 28 日</div>

    <div class="text-base text-gray-300 leading-relaxed space-y-6">
      <p>浏览器原生能力在 2026 年已经足够强大。不需要 JavaScript，只用 CSS 自定义属性和 <code class="bg-white/5 px-1.5 py-0.5 rounded font-mono text-sm text-amber-400">:has()</code> 选择器，就能做出漂亮的明暗主题切换——而且过渡动画是丝滑的。</p>

      <h3 class="font-serif text-2xl font-semibold text-white mt-12 mb-4">核心思路</h3>
      <p>把主题状态存在一个 CSS 自定义属性里，用 <code class="bg-white/5 px-1.5 py-0.5 rounded font-mono text-sm text-amber-400">:has()</code> 选择器让任意子元素响应这个状态变化。配合 <code class="bg-white/5 px-1.5 py-0.5 rounded font-mono text-sm text-amber-400">light-dark()</code> 函数自动切换色值。</p>

      <pre class="bg-black/40 border border-border rounded-xl p-5 md:p-6 overflow-x-auto"><code class="font-mono text-sm text-gray-200 leading-relaxed">{code1}</code></pre>

      <h3 class="font-serif text-2xl font-semibold text-white mt-12 mb-4">切换按钮</h3>
      <p>一个 checkbox，配上一个漂亮的开关样式。<code class="bg-white/5 px-1.5 py-0.5 rounded font-mono text-sm text-amber-400">:has()</code> 检测勾选状态，全局主题立马切换。整个过程零 JavaScript。</p>

      <pre class="bg-black/40 border border-border rounded-xl p-5 md:p-6 overflow-x-auto"><code class="font-mono text-sm text-gray-200 leading-relaxed" set:html={code2} /></pre>

      <h3 class="font-serif text-2xl font-semibold text-white mt-12 mb-4">过渡动画</h3>
      <p>加上 <code class="bg-white/5 px-1.5 py-0.5 rounded font-mono text-sm text-amber-400">transition</code> 让颜色平滑过渡。0.3 秒的 ease，切换时背景和文字渐变，体验丝滑。</p>

      <p>最终效果：一个纯 CSS 的主题切换，不到 50 行代码，性能完美，可访问性无懈可击。这就是 2026 年前端的美妙之处——平台本身已经给了你足够多的东西。</p>
    </div>

    <div class="mt-16 pt-8 border-t border-border">
      <a href="/blog" class="text-xs text-text-dim no-underline hover:text-white transition-colors">← 返回 Blog</a>
    </div>
  </article>
</BaseLayout>
